<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.application.audit.module.user.dao.FileBatisDao">
    <insert id="saveFile" parameterType="FileBO">
        insert into t_file (file_url,user_id) values(#{fileUrl},#{userId})
    </insert>
</mapper>
